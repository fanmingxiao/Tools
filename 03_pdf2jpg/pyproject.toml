[project]
name = "pdf2jpg"
version = "1.0.0"
description = "PDF批量转JPG工具"
requires-python = ">=3.9"
dependencies = [
    "pdf2image>=1.16.0",
    "Pillow>=10.0.0",
    "pyinstaller>=6.18.0",
]

[tool.uv]
dev-dependencies = []
